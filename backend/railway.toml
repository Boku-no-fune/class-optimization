[build]
builder = "NIXPACKS"

[deploy]
startCommand = "bundle exec rails db:create db:migrate && bundle exec rails server -p $PORT -b 0.0.0.0"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
